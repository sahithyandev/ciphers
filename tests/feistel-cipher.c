// Tests for src/feistel-cipher.c, run via `make test`.
#include <string.h>
#include "test.h"
#include "../src/feistel-cipher.c"

int main(void) {
    TEST_INIT();

    // TODO: add tests, e.g.:
    // char msg[] = "...";
    // feistel_cipher(msg, 0);
    // CHECK(strcmp(msg, "...") == 0);

    return TEST_SUMMARY();
}
